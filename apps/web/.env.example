# InterviewOS 前端环境变量示例（生产环境必须设置）
# 复制为 .env.local 并填入实际值；不要提交真实配置到仓库。

# 端口规划（默认聚合形态）：前端 8080 / 后端单进程聚合 8081；
# 后端各服务独立运行时顺延 8081(api) / 8082(agent) / 8083(interview)。

# 后端 REST API 基础地址（前端 api.ts 直连后端，不经 Next 代理）
NEXT_PUBLIC_API_BASE=http://localhost:8081

# WebSocket 直连地址（绕过 Next 代理，避免 WS upgrade 兼容问题）
NEXT_PUBLIC_WS_URL=ws://localhost:8081

# 流式接口（SSE）的直连地址，必须与 NEXT_PUBLIC_API_BASE 同源；单独设置以绕过代理
NEXT_PUBLIC_STREAM_API_BASE=http://localhost:8081
